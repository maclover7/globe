<%= form_for(@invite_code) do |f| %>
  <% if @invite_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invite_code.errors.count, "error") %> prohibited this invite_code from being saved:</h2>

      <ul>
      <% @invite_code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.check_box :for_school_admins %>
    <%= f.label :for_school_admins, "If checked, this code can be used to register as a school admin" %>
  </div>

  <div class="form-group">
    <%= f.check_box :for_teachers %>
    <%= f.label :for_teachers, "If checked, this code can be used to register as a teacher" %>
  </div>

  <div class="form-group">
    <%= f.submit class: "btn btn-primary btn-lg" %>
  </div>
<% end %>
